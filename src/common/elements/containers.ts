import styled from "styled-components"
import { SIZE } from "../constants"

export const Container = styled.div`
  width: 100%;
  max-width: ${SIZE.MAX_WIDTH};
  padding-right: 15px;
  padding-left: 15px;
  margin-right: auto;
  margin-left: auto;
`
export const MainContainer = styled.div`
  padding: ${SIZE.HEADER_HIGHT} 0 0 0;
`

